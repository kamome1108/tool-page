export const relatedTools: Record<string, string[]> = {
    'json-formatter': [
        'json-minifier',
        'json-to-yaml',
        'yaml-to-json',
        'xml-formatter'
    ],
    // Add other tools here as we implement them
};
